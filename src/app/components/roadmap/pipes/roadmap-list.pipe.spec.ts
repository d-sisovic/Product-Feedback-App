import { RoadmapListPipe } from './roadmap-list.pipe';

describe('RoadmapListPipe', () => {
  it('create an instance', () => {
    const pipe = new RoadmapListPipe();
    expect(pipe).toBeTruthy();
  });
});
