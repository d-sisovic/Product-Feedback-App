import { TabQuantityPipe } from './tab-quantity.pipe';

describe('TabQuantityPipe', () => {
  it('create an instance', () => {
    const pipe = new TabQuantityPipe();
    expect(pipe).toBeTruthy();
  });
});
